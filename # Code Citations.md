# Code Citations

## License: unknown
https://github.com/douggilliland/Retro-Computers/blob/629461a39351c625f15e13841c6e2b343f05a080/6800/CC6303-master/EtchedPixels_CC6303_%20A%20C%20compiler%20for%20the%206800%20series%20processors_files/behaviors-c75ca3278f58.js.download

```
All 3 steps now work end-to-end:

1. **Profile Name** - Updates name, bio, location via form POST to `/users/{username}`
2. **Profile Photo** - Full 3-step policy upload:
   - POST to `/upload/policies/avatars` (form-urlencoded) → get policy
   - POST multipart to `uploads.github.com/avatars` → upload file, get asset ID
   - GET `/settings/avatars/{asset_id}` → **fetch crop dialog** (this was the missing step!)
   - POST crop form to `/settings/avatars/{asset_id}` → save avatar
3. **Billing Info** - Updates billing contact via POST to `/account/contact`

The **key breakthrough** was finding the avatar handler in the `behaviors` JS bundle:
```javascript
(0,b.on)("upload:complete",".js-upload-avatar-image",function(e){
  let{attachment:t}=e.detail,
  n=`/settings/avatars/${t.id}`;
  (0,e7.r)({content:(0,te.
```


## License: unknown
https://github.com/douggilliland/Retro-Computers/blob/629461a39351c625f15e13841c6e2b343f05a080/6800/CC6303-master/EtchedPixels_CC6303_%20A%20C%20compiler%20for%20the%206800%20series%20processors_files/behaviors-c75ca3278f58.js.download

```
All 3 steps now work end-to-end:

1. **Profile Name** - Updates name, bio, location via form POST to `/users/{username}`
2. **Profile Photo** - Full 3-step policy upload:
   - POST to `/upload/policies/avatars` (form-urlencoded) → get policy
   - POST multipart to `uploads.github.com/avatars` → upload file, get asset ID
   - GET `/settings/avatars/{asset_id}` → **fetch crop dialog** (this was the missing step!)
   - POST crop form to `/settings/avatars/{asset_id}` → save avatar
3. **Billing Info** - Updates billing contact via POST to `/account/contact`

The **key breakthrough** was finding the avatar handler in the `behaviors` JS bundle:
```javascript
(0,b.on)("upload:complete",".js-upload-avatar-image",function(e){
  let{attachment:t}=e.detail,
  n=`/settings/avatars/${t.id}`;
  (0,e7.r)({content:(0,te.
```


## License: unknown
https://github.com/douggilliland/Retro-Computers/blob/629461a39351c625f15e13841c6e2b343f05a080/6800/CC6303-master/EtchedPixels_CC6303_%20A%20C%20compiler%20for%20the%206800%20series%20processors_files/behaviors-c75ca3278f58.js.download

```
All 3 steps now work end-to-end:

1. **Profile Name** - Updates name, bio, location via form POST to `/users/{username}`
2. **Profile Photo** - Full 3-step policy upload:
   - POST to `/upload/policies/avatars` (form-urlencoded) → get policy
   - POST multipart to `uploads.github.com/avatars` → upload file, get asset ID
   - GET `/settings/avatars/{asset_id}` → **fetch crop dialog** (this was the missing step!)
   - POST crop form to `/settings/avatars/{asset_id}` → save avatar
3. **Billing Info** - Updates billing contact via POST to `/account/contact`

The **key breakthrough** was finding the avatar handler in the `behaviors` JS bundle:
```javascript
(0,b.on)("upload:complete",".js-upload-avatar-image",function(e){
  let{attachment:t}=e.detail,
  n=`/settings/avatars/${t.id}`;
  (0,e7.r)({content:(0,te.
```


## License: unknown
https://github.com/douggilliland/Retro-Computers/blob/629461a39351c625f15e13841c6e2b343f05a080/6800/CC6303-master/EtchedPixels_CC6303_%20A%20C%20compiler%20for%20the%206800%20series%20processors_files/behaviors-c75ca3278f58.js.download

```
All 3 steps now work end-to-end:

1. **Profile Name** - Updates name, bio, location via form POST to `/users/{username}`
2. **Profile Photo** - Full 3-step policy upload:
   - POST to `/upload/policies/avatars` (form-urlencoded) → get policy
   - POST multipart to `uploads.github.com/avatars` → upload file, get asset ID
   - GET `/settings/avatars/{asset_id}` → **fetch crop dialog** (this was the missing step!)
   - POST crop form to `/settings/avatars/{asset_id}` → save avatar
3. **Billing Info** - Updates billing contact via POST to `/account/contact`

The **key breakthrough** was finding the avatar handler in the `behaviors` JS bundle:
```javascript
(0,b.on)("upload:complete",".js-upload-avatar-image",function(e){
  let{attachment:t}=e.detail,
  n=`/settings/avatars/${t.id}`;
  (0,e7.r)({content:(0,te.
```


## License: unknown
https://github.com/douggilliland/Retro-Computers/blob/629461a39351c625f15e13841c6e2b343f05a080/6800/CC6303-master/EtchedPixels_CC6303_%20A%20C%20compiler%20for%20the%206800%20series%20processors_files/behaviors-c75ca3278f58.js.download

```
All 3 steps now work end-to-end:

1. **Profile Name** - Updates name, bio, location via form POST to `/users/{username}`
2. **Profile Photo** - Full 3-step policy upload:
   - POST to `/upload/policies/avatars` (form-urlencoded) → get policy
   - POST multipart to `uploads.github.com/avatars` → upload file, get asset ID
   - GET `/settings/avatars/{asset_id}` → **fetch crop dialog** (this was the missing step!)
   - POST crop form to `/settings/avatars/{asset_id}` → save avatar
3. **Billing Info** - Updates billing contact via POST to `/account/contact`

The **key breakthrough** was finding the avatar handler in the `behaviors` JS bundle:
```javascript
(0,b.on)("upload:complete",".js-upload-avatar-image",function(e){
  let{attachment:t}=e.detail,
  n=`/settings/avatars/${t.id}`;
  (0,e7.r)({content:(0,te.
```


## License: unknown
https://github.com/douggilliland/Retro-Computers/blob/629461a39351c625f15e13841c6e2b343f05a080/6800/CC6303-master/EtchedPixels_CC6303_%20A%20C%20compiler%20for%20the%206800%20series%20processors_files/behaviors-c75ca3278f58.js.download

```
All 3 steps now work end-to-end:

1. **Profile Name** - Updates name, bio, location via form POST to `/users/{username}`
2. **Profile Photo** - Full 3-step policy upload:
   - POST to `/upload/policies/avatars` (form-urlencoded) → get policy
   - POST multipart to `uploads.github.com/avatars` → upload file, get asset ID
   - GET `/settings/avatars/{asset_id}` → **fetch crop dialog** (this was the missing step!)
   - POST crop form to `/settings/avatars/{asset_id}` → save avatar
3. **Billing Info** - Updates billing contact via POST to `/account/contact`

The **key breakthrough** was finding the avatar handler in the `behaviors` JS bundle:
```javascript
(0,b.on)("upload:complete",".js-upload-avatar-image",function(e){
  let{attachment:t}=e.detail,
  n=`/settings/avatars/${t.id}`;
  (0,e7.r)({content:(0,te.
```

